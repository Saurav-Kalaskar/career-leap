---
name: latex-pdf
description: Generate ATS‑optimized one‑page PDF resume using LaTeX template. Accepts JD text or URL, auto‑extracts company name, bolds JD keywords.
user_invocable: true
args: --jd
argument-hint: "<text|url>"
---

# latex-pdf – Router

## Mode Routing

`latex-pdf` maps to LaTeX PDF generation.

## Execution

Runs `node generate-latex-pdf.mjs --jd <text|url>`.
